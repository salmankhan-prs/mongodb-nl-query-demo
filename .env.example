# Database Configuration
MONGODB_URI=mongodb://localhost:27017/mongodb-nl-demo

# Optional: Redis for session persistence
REDIS_URL=redis://localhost:6379

# Memory management
# Choose between "redis" and "memory"
# If using "redis", ensure REDIS_URL is set.
MEMORY_TYPE=memory
MEMORY_CONNECTION_STRING=

# Server
PORT=3000
NODE_ENV=development

# Session Configuration
SESSION_TTL=3600
ANTHROPIC_API_KEY=your_anthropic_api_key_here
